# V2.0 登录升级

- 取消一次性初始化码，首次打开直接设置管理密码。
- 管理员初始化使用 D1 条件写入，避免两台设备同时设置时互相覆盖。
- 管理密码限制为 10–128 位。
- 初始化成功后自动创建 30 天登录会话。
- 更新 Service Worker 缓存策略，JavaScript/CSS 优先读取最新网络版本，减少升级后仍显示旧页面的问题。
- 保留现有 D1、R2、商品、库存、销售、借调、合同和报表数据，不清空数据库。
