# v2.8 秦丝数据导入中心

- 支持直接读取秦丝 `.xlsx` 和 `.csv` 文件，不需要第三方转换网站。
- 自动识别商品、客户、库存和销售文件。
- 商品按货号去重更新，客户按手机或姓名合并。
- 库存按货号汇总仓库后设置为当前库存，并生成同步流水。
- 销售作为历史记录导入，不再次扣减当前库存。
- 导入前预览新增、更新、警告、无效和跳过数量。
- 同一文件重复导入时不会重复增加销售单。
- 导入失败自动恢复到导入前的本机快照。
- 导入完成后生成结果 CSV，并同步到 Cloudflare。
- 商品图片先使用秦丝图片链接显示。
