# v3.6 — iPhone 快捷指令桥

- 新增「漠翠保存素材」快捷指令桥，PWA 不再依赖 Safari 批量下载/分享权限。
- 内容工作台「下载」：原图 + 原视频生成 20 分钟临时任务，直接运行快捷指令保存到照片。
- 「店铺1:1」：浏览器完整构图生成全部 1:1 图片后，临时上传 R2，再交给快捷指令逐张写入照片。
- 「朋友圈」：复制朋友圈文案、记录分享时间、保存全部原图，随后由快捷指令打开微信。
- 新增「更多 → iPhone快捷保存」一次性设置页面。
- 三种动作合并为一个快捷指令 `漠翠保存素材`，只需配置一次。
- 快捷任务使用随机 token；原始 token 不写入 D1，20 分钟自动失效。
- 店铺 1:1 临时文件仅存在 `shortcut-temp/`，任务过期后自动清理。
- 桌面/非 iOS 浏览器保留原来的 Web Share 兼容流程。
