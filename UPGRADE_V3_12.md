# 漠翠进销存 v3.12.0 — 配饰库存

## 覆盖文件
只覆盖 `public/` 下 3 个文件：
- `app.js`
- `sales-cost-v3.js`
- `sw.js`

## 数据安全设计
- **不升级 IndexedDB DB_VERSION**，不创建/删除 object store。
- 配饰库存使用现有 `settings` store 的 `accessoryInventoryV1` 账本，因此现有 JSON 备份、Cloudflare 增量同步和恢复流程天然包含配饰数据。
- 不迁移、不重写历史商品、销售、调借、库存流水。
- 旧销售没有新字段时按 0 成本兼容。
- 配饰不物理删除：库存归零后可“停用”，历史销售与流水永久保留。

## 新功能
1. 设置 → 配饰库存：配饰 SKU、类别、单位、库存、单位成本、备注。
2. 采购/补充入库使用移动加权平均成本；手工出库/损耗保留流水。
3. 销售开单可选择库存配饰，自动计成本、扣库存。
4. 低价值绳子/包装使用“低价值配饰成本”直接录入，不占配饰 SKU。
5. 其他直接成本独立录入。
6. 撤销销售自动退回配饰；恢复销售先校验库存再重新扣减。
7. 报表 `saleCostTotal()` 已纳入配饰成本和其他直接成本。
8. 复制撤销单时不自动复制库存配饰，必须重新选择，防止误扣。

## 部署前
建议先在系统“设置 → 导出完整 JSON 备份”保存一份当前账本。此建议是额外保险，本版本身没有数据迁移步骤。
