# 从旧版迁移数据

旧版浏览器或本机版本的数据不会自动出现在新域名。

## 在旧版导出

1. 打开旧版进销存。
2. 进入 `更多 → 数据与设置`。
3. 点击 `导出完整 JSON 备份`。
4. 保存文件到 Mac 或 iCloud。

## 在 Cloudflare 最终版导入

1. 登录新系统。
2. 进入 `更多 → 数据与设置`。
3. 点击 `从 JSON 备份恢复`。
4. 选择旧版导出的 JSON。
5. 确认恢复。
6. 点击 `立即同步`。

导入后检查：商品总数、库存总数量、销售单数量、调借未还数量和最近一次销售。
