# v3.1.2 合并更新包

包含尚未部署的 v3.1.1 全部内容：
- 商品详情销售/调借重复显示修复
- 更多页 3 列紧凑入口
- 商品详情不显示逾期红色

新增 v3.1.2：
- 调借“全部”默认分为“正在借调”和“已还清”
- 正在借调永远置顶
- 正在借调按调借日期最新 → 最早
- 部分归还但仍有剩余数量，继续留在正在借调
- 全部归还/全部处理完成后自动下沉到已还清
- 已还清按完成时间最新 → 最早
- 超期红色仍只在调借页面显示

这是完整合并包，可直接从 v3.1 升级，无需先装 v3.1.1。
不修改正式商品、库存、销售、调借记录，只改变显示和排序。
