# v2.4 测试报告

测试日期：2026-08-02

## 已完成检查

- `public/app.js` 通过 Node.js 语法检查。
- `public/cloud.js` 通过 Node.js 语法检查。
- `src/index.js` 通过 Node.js 语法检查。
- IndexedDB 版本已从 1 调整为 2，并保留原有全部数据表，只增加 `auditLogs`。
- 云端同步导入、导出会包含新增的 `auditLogs` 数据表。
- Service Worker 缓存名称已更新为 `mocui-inventory-pwa-v2-4-0`。
- 登录设备接口包含：设备列表、退出其他设备、退出指定设备。
- 库存体检只读取商品和库存流水；“补齐流水”不会直接改变商品当前库存。
- 旧借调单缺少预计归还日期时具备 30 天兼容回退逻辑。
- 补丁未包含 `wrangler.jsonc`，不会覆盖当前 D1、R2 或域名配置。

## 需要部署后验证

以下项目依赖你的真实 Cloudflare D1、R2、域名和现有数据，必须在部署后由实际环境验证：

- 真实多设备会话列表和远程退出。
- 原有大量库存流水在“库存体检”中的结果。
- iPhone PWA 缓存更新和左上角返回操作。
- 云端同步冲突、历史版本恢复和大图片数据同步。

建议先用 2—3 件测试商品完成：新增、销售、撤销、恢复、借出、部分归还、借调售出和盘点，再逐步录入全部正式库存。
