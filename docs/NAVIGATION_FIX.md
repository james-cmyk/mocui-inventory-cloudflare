# v2.3 新增借调返回修复

本补丁修复：打开“新增借调”后缺少明确返回入口，只能刷新页面返回。

## 修复内容

- 新增借调页右上角显示“← 返回”。
- 点击返回会回到原借调列表，不刷新页面，不丢失数据库数据。
- 支持 iPhone Safari/PWA 左滑返回和浏览器返回键关闭当前弹层。
- 保存借调单后仍自动回到借调列表。
- 更新 Service Worker 缓存版本，避免手机继续使用旧版 app.js。

## 上传

上传 `public/app.js` 与 `public/sw.js` 到 GitHub，覆盖同名文件并提交。
