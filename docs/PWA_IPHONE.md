# iPhone PWA 安装说明

1. 等 Cloudflare 新构建部署成功。
2. 使用 iPhone 的 Safari 打开正式域名。
3. 点击 Safari 底部“分享”。
4. 选择“添加到主屏幕”。
5. 点击右上角“添加”。

以后从桌面图标打开，即为全屏 PWA 模式。版本升级后，页面会提示“发现新版本”，点击“立即更新”即可。

本补丁仅更新 `public` 静态资源，不修改 D1、R2、登录密码、库存或业务数据，也不覆盖 `wrangler.jsonc`。
